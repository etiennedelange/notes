[package]
name = "notes-core"
version = "0.1.0"
description = "Filesystem, consent and persistence core for Notes, independent of any UI toolkit"
authors = ["Etienne de Lange"]
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
