//! UI-free helpers ported from the TypeScript frontend (`src/*.ts`). Every
//! function here is pure so its vitest cases can come across as `#[test]`s.

pub mod doc_stats;
pub mod fuzzy;
pub mod paste_code;
pub mod pathutil;
